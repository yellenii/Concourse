---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

  #inputs:
  #  - name: resource-tutorial
  #  - name: timer

run:
  path: /bin/sh
  args: ["./concourse/web-task.sh"]
