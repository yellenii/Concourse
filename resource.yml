---
platform: linux

image_resource:
  type: docker-image
  source: {repository: helloyeeun}

inputs:
  - name: resource-tutorial

run:
  path: echo
  args: [deployed!]
        #  path: sh
        #  args: ["./resource-tutorial/web-task.sh"]
